# SPA fallback: serve index.html for all routes so React Router can handle /trades/* etc.
# (Static files like sitemap.xml, robots.txt are served by Netlify from netlify.toml first.)
/*    /index.html   200
