# SPA fallback is handled by Cloudflare via wrangler "not_found_handling": "single-page-application".
# Do not add a catch-all rule here (e.g. /* -> /index.html 200) — it causes an infinite redirect loop on Cloudflare.
# For Netlify, use netlify.toml redirects instead.
